spring:
  application:
    name: api-gateway-test
  main:
    allow-bean-definition-overriding: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri:
  cloud:
    gateway:
      routes:
        - id: test-route
          uri: http://localhost:9999
          predicates:
            - Path=/test/**
  redis:
    host: localhost
    port: 6379

server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

# Resilience4j Circuit Breaker Configuration for Testing
resilience4j:
  circuitbreaker:
    configs:
      default:
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 1s  # Shorter for testing
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        recordExceptions:
          - org.springframework.web.client.HttpServerErrorException
          - java.util.concurrent.TimeoutException
          - java.io.IOException
    instances:
      orderServiceCircuitBreaker:
        baseConfig: default
      consumerServiceCircuitBreaker:
        baseConfig: default
      restaurantServiceCircuitBreaker:
        baseConfig: default
      kitchenServiceCircuitBreaker:
        baseConfig: default
      deliveryServiceCircuitBreaker:
        baseConfig: default
      orderHistoryServiceCircuitBreaker:
        baseConfig: default

  timelimiter:
    configs:
      default:
        timeoutDuration: 5s

# Service URLs for API composition testing
services:
  order-service:
    url: http://localhost:8091
  kitchen-service:
    url: http://localhost:8092
  delivery-service:
    url: http://localhost:8093
