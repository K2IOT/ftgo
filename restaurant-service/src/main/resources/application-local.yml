spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3308/ftgo_restaurant?createDatabaseIfNotExist=true
    username: ftgo_user
    password: ftgo_password
