spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3307/ftgo_consumer?createDatabaseIfNotExist=true
    username: ftgo_user
    password: ftgo_password
