spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306/ftgo_order?createDatabaseIfNotExist=true
    username: ftgo_user
    password: ftgo_password
