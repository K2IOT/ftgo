name: Phase 04 Security and API

on:
  pull_request:
    branches: [dev]
    paths:
      - 'common/**'
      - 'api-gateway/**'
      - 'order-service/**'
      - 'consumer-service/**'
      - 'restaurant-service/**'
      - 'kitchen-service/**'
      - 'accounting-service/**'
      - 'delivery-service/**'
      - 'order-history-service/**'
      - 'e2e-tests/**'
      - 'deployment/tests/test_phase04_*.py'
      - '.github/workflows/phase-04-security-api.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase-04-security-api-${{ github.ref }}
  cancel-in-progress: true

jobs:
  security-architecture-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Java 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Verify Gradle wrapper
        run: bash scripts/ci/verify-gradle-wrapper.sh

      - name: Verify resource server architecture contract
        run: python -m unittest deployment.tests.test_phase04_resource_server_contract -v

      - name: Compile gateway and every HTTP service
        run: |
          ./gradlew \
            :common:compileJava \
            :api-gateway:compileJava \
            :order-service:compileJava \
            :consumer-service:compileJava \
            :restaurant-service:compileJava \
            :kitchen-service:compileJava \
            :accounting-service:compileJava \
            :delivery-service:compileJava \
            :order-history-service:compileJava \
            --no-daemon --stacktrace

      - name: Verify shared JWT principal contract
        run: ./gradlew :common:test --tests '*FtgoJwtAuthenticationConverterTest' --no-daemon --stacktrace

      - name: Upload shared security diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: phase-04-shared-security-diagnostics
          if-no-files-found: warn
          path: |
            common/build/reports/tests/**
            common/build/test-results/**

  service-security-smoke:
    name: service-security-smoke (${{ matrix.service }})
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        service:
          - order-service
          - consumer-service
          - restaurant-service
          - kitchen-service
          - accounting-service
          - delivery-service
          - order-history-service
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Java 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Verify Gradle wrapper
        run: bash scripts/ci/verify-gradle-wrapper.sh

      - name: Verify resource server behavior
        shell: bash
        run: |
          set -o pipefail
          ./gradlew :${{ matrix.service }}:test \
            --tests '*SecuritySmokeTest' \
            --no-daemon --console=plain --stacktrace \
            2>&1 | tee phase-04-${{ matrix.service }}-security.log

      - name: Upload service security diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: phase-04-${{ matrix.service }}-security-diagnostics
          if-no-files-found: warn
          path: |
            phase-04-${{ matrix.service }}-security.log
            ${{ matrix.service }}/build/reports/tests/**
            ${{ matrix.service }}/build/test-results/**

  order-ownership-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Java 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Verify Gradle wrapper
        run: bash scripts/ci/verify-gradle-wrapper.sh

      - name: Verify order ownership and trusted consumer identity
        run: |
          ./gradlew :order-service:test \
            --tests '*OrderAuthorizationServiceTest' \
            --tests '*OrderControllerIdentityTest' \
            --no-daemon --stacktrace

      - name: Upload order ownership diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: phase-04-order-ownership-diagnostics
          if-no-files-found: warn
          path: |
            order-service/build/reports/tests/**
            order-service/build/test-results/**

  consumer-ownership-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Java 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Verify Gradle wrapper
        run: bash scripts/ci/verify-gradle-wrapper.sh

      - name: Verify consumer ownership and privileged credit policy
        shell: bash
        run: |
          set -o pipefail
          ./gradlew :consumer-service:test \
            --tests '*ConsumerAuthorizationServiceTest' \
            --tests '*ConsumerControllerAuthorizationTest' \
            --tests '*ConsumerAdminControllerTest' \
            --no-daemon --console=plain --stacktrace \
            2>&1 | tee phase-04-consumer-ownership.log

      - name: Upload consumer ownership diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: phase-04-consumer-ownership-diagnostics
          if-no-files-found: warn
          path: |
            phase-04-consumer-ownership.log
            consumer-service/build/reports/tests/**
            consumer-service/build/test-results/**

  gateway-composition-security-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Java 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Verify Gradle wrapper
        run: bash scripts/ci/verify-gradle-wrapper.sh

      - name: Verify composed calls relay the authenticated bearer token
        run: |
          ./gradlew :api-gateway:test \
            --tests '*DownstreamTokenRelayTest' \
            --no-daemon --console=plain --stacktrace

      - name: Upload gateway composition diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: phase-04-gateway-composition-diagnostics
          if-no-files-found: warn
          path: |
            api-gateway/build/reports/tests/**
            api-gateway/build/test-results/**

  consumer-test-compile-diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Print isolated consumer compile failure
        shell: bash
        run: |
          set -euo pipefail
          git clone --depth 1 --branch "${{ github.event.pull_request.head.ref }}" \
            https://github.com/${{ github.repository }}.git repository >/dev/null 2>&1
          cd repository
          export JAVA_HOME="$JAVA_HOME_21_X64"
          export PATH="$JAVA_HOME/bin:$PATH"
          if ./gradlew :consumer-service:compileTestJava \
              --no-daemon --console=plain >/tmp/consumer-compile.log 2>&1; then
            echo "consumer test compilation passed"
          else
            grep -E "(^| )error:|FAILED|FAILURE:|Execution failed|Compilation failed|Could not" \
              /tmp/consumer-compile.log | tail -n 80
            exit 1
          fi
