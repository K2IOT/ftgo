spring:
  application:
    name: order-history-service-test
  cassandra:
    contact-points: localhost
    port: 9042
    keyspace-name: ftgo_order_history_test
    local-datacenter: datacenter1
    schema-action: create-if-not-exists
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: order-history-service-test

server:
  port: 0

logging:
  level:
    net.ftgo.orderhistory: DEBUG
