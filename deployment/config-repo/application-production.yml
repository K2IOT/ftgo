# Production profile configuration
spring:
  kafka:
    bootstrap-servers: kafka-prod-1:9092,kafka-prod-2:9093,kafka-prod-3:9094
    producer:
      acks: all
      retries: 3
    consumer:
      enable-auto-commit: false

logging:
  level:
    net.ftgo: INFO
    org.springframework: WARN
