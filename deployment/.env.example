# Local development values only. Copy to deployment/.env and replace before use.
# Production deployments must source these values from an external secret store.
FTGO_MYSQL_ROOT_PASSWORD=change-me-local-root
FTGO_MYSQL_USER=ftgo_user
FTGO_MYSQL_PASSWORD=change-me-local-user
FTGO_VAULT_DEV_ROOT_TOKEN=change-me-local-vault-token
