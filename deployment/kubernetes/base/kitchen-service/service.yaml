apiVersion: v1
kind: Service
metadata:
  name: kitchen-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kitchen-service
  ports:
    - name: http
      port: 8080
      targetPort: http
