apiVersion: v1
kind: Service
metadata:
  name: consumer-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: consumer-service
  ports:
    - name: http
      port: 8080
      targetPort: http
