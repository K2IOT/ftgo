apiVersion: v1
kind: Service
metadata:
  name: accounting-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: accounting-service
  ports:
    - name: http
      port: 8080
      targetPort: http
