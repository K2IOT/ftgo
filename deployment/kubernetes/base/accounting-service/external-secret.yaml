apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: accounting-service-runtime
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: ftgo-vault
    kind: ClusterSecretStore
  target:
    name: accounting-service-runtime
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: SPRING_DATASOURCE_USERNAME
      remoteRef:
        key: production/accounting-service
        property: database.username
    - secretKey: SPRING_DATASOURCE_PASSWORD
      remoteRef:
        key: production/accounting-service
        property: database.password
    - secretKey: SPRING_KAFKA_PROPERTIES_SASL_JAAS_CONFIG
      remoteRef:
        key: production/accounting-service
        property: kafka.sasl-jaas-config
    - secretKey: FTGO_PAYMENT_PROVIDER_API_KEY
      remoteRef:
        key: production/accounting-service
        property: payment.provider-api-key
    - secretKey: FTGO_SECURITY_ISSUER_URI
      remoteRef:
        key: production/accounting-service
        property: security.issuer-uri
    - secretKey: FTGO_SECURITY_JWK_SET_URI
      remoteRef:
        key: production/accounting-service
        property: security.jwk-set-uri
