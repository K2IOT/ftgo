apiVersion: v1
kind: Service
metadata:
  name: order-history-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: order-history-service
  ports:
    - name: http
      port: 8080
      targetPort: http
